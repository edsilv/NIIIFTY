rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // todo: disallow access to default file?
      allow read;
      allow write: if request.auth!=null;
    }
  }
}
